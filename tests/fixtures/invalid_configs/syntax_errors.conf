# Configuration with various syntax errors for testing error handling

# Missing opening brace
HANDLER vdisk_fileio
    DEVICE disk1 {
        filename /path/to/disk1.img
    }
}

# Missing closing brace
HANDLER dev_disk {
    DEVICE sda {
        filename /dev/sda
    
}

# Invalid block type
INVALID_BLOCK_TYPE test {
    some_attr value
}

# Malformed LUN assignment
TARGET_DRIVER iscsi {
    TARGET iqn.2024-01.com.example:bad {
        LUN invalid_lun_id disk1
        enabled 1
    }
}

# Unclosed quotes
HANDLER vdisk_fileio {
    DEVICE bad_quotes {
        filename "/unclosed/quote/path
    }
}

# Empty block
HANDLER empty_handler {
}